---
'@salesforce/b2c-agent-plugins': patch
---

Update the b2c-config skill's IDE integration section to reference `setup ide vscode-types` and `setup ide tsserver-plugin` for Script API IntelliSense.
