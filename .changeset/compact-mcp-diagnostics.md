---
'@salesforce/b2c-dx-mcp': major
'@salesforce/b2c-dx-docs': patch
'@salesforce/b2c-tooling-sdk': patch
'@salesforce/b2c-agent-plugins': patch
---

Consolidate debugger execution and inspection into `debug_control` and `debug_inspect`, and log-watch lifecycle operations into `logs_watch` and `mrt_logs_watch`. Use these names in explicit tool selections. Simplify the tool reference to capabilities, exact names, and required access.
