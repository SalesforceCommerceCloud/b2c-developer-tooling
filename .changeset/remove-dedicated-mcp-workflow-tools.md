---
'@salesforce/b2c-dx-mcp': major
'@salesforce/b2c-dx-docs': patch
---

Remove `pwakit_get_guidelines` and its bundled content, along with `scapi_custom_api_generate_scaffold`. Remove these names from explicit tool selections. Use `b2c scaffold generate custom-api` for local API scaffolding.
