---
'@salesforce/b2c-dx-mcp': minor
---

Enable all toolsets by default, independent of the server's launch directory. Use `--toolsets` or `--tools` to restrict available capabilities. Documentation tools can detect the storefront from a project directory supplied for the current task; bundled skills are directory-independent.
