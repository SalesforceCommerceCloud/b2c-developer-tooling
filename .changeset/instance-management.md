---
'@salesforce/b2c-cli': minor
'@salesforce/b2c-tooling-sdk': minor
---

Add instance management commands for configuring B2C Commerce instances.

- Renamed `setup config` to `setup inspect`
- Added `setup instance list` to view all configured instances
- Added `setup instance create` to add new instance configurations
- Added `setup instance remove` to delete instance configurations
- Added `setup instance set-active` to set the default instance
