---
'@salesforce/b2c-dx-mcp': patch
'@salesforce/b2c-dx-docs': patch
'b2c-vs-extension': patch
---

Remove the MCP preview-tool launch gate. SCAPI code mode and metrics are available through normal tool selection; API credentials and tenant access requirements still apply. Remove the obsolete preview flag from existing launch commands; bundled plugin setup and VS Code installation commands are updated.
