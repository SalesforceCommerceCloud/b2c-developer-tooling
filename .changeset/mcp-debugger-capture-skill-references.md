---
'@salesforce/b2c-dx-mcp': patch
---

Return debugger captures while their HTTP trigger is halted so the session can be resumed without blocking the capture call. Configuration warnings, unmapped breakpoints, and capture timeouts now link to focused troubleshooting skills; normal results stay concise.
