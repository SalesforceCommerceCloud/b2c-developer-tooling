---
'@salesforce/b2c-tooling-sdk': patch
'@salesforce/b2c-dx-docs': patch
---

Document Cursor as a first-class skills target, including its compatibility with Claude Code and Codex skill paths so plugins installed via `claude plugin install` are auto-picked-up by Cursor.
