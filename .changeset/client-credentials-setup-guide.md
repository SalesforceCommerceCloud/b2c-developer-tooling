---
'@salesforce/b2c-dx-docs': patch
---

Add a concise Client Credentials Setup guide for internal Salesforce developers, covering the Confidential Account Manager client (pod5), `client_secret_basic` token endpoint auth, client roles, and environment variables for non-interactive/CI use.
