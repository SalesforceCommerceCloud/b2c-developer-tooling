---
'@salesforce/b2c-cli': patch
'@salesforce/b2c-tooling-sdk': patch
---

Show the target hostname and real local source for each import-set item, and reject empty site archive directories before upload with a clear error.
