---
'@salesforce/b2c-dx-mcp': patch
'@salesforce/b2c-tooling-sdk': patch
---

Simplify SCAPI code mode with directly readable schemas, tool-local examples, and a single JSON result that preserves target and error details without duplicate output.
